// Import
import app from "./app";
// Mongodb
// Middlewares
// Routes
// Error
// Catch error
// Listen
const PORT = 3000;

app.listen(PORT, () => {
    console.log(`Server is running on port: ${PORT}`);
});